{
  "name": "vercel-corepack-test",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run test"
  },
  "engines": {
    "node": ">=18",
    "pnpm": "9.0.4"
  },
  "packageManager": "pnpm@9.0.4",
  "devDependencies": {
    "turbo": "^1.13.3"
  }
}
